#include "atkui/framework.h"
using namespace glm;


// virtual void scene() {
    
//     for(float i=0.5;i<40;i++){
//         for(float j=0.5;j<40;j++){
//             mainAngle = atan2(_mouseY, _mouseX);
//             int dx = mousePosition().x - _mouseX;
//             int dy = mousePosition().y - _mouseY;
//             mouseMotion(mousePosition().x, mousePosition().y, dx, dy);
//             vec3 target = vec3(_mouseX, _mouseY, 0);
//             setColor(vec3(1,0,0));
//             drawSphere(target, 15);
//             int newPosY = 50*sin(mainAngle) + 0.5 * height();
//             int newPosX = 50*cos(mainAngle) + 0.5 * width();
//             setColor(vec3(0,1,0));
//             drawCube(vec3(newPosX*i*0.1, newPosY*j*0.1, 0), vec3(30, 3, 0));
//         }
//     }
// }



int main(int argc, char** argv) {
   return 0;
}

